{
  "name": "babel-test",
  "version": "1.0.0",
  "description": "",
  "scripts": {
		"build": "parcel build js/**/*.js -d js-build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@babel/core": "^7.10.2",
    "@babel/preset-env": "^7.10.2"
  }
}
